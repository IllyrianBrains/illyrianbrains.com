const fetch = require("node-fetch");

module.exports = async function () {
  // const url = "https://forum.illyrianbrains.dev/c/events.json"; // replace this
  // const res = await fetch(url);
  // const data = await res.json();

  // Customize parsing to extract relevant event info
  //return data.topic_list.topics.map(topic => ({
   // title: topic.title,
    //url: `https://forum.illyrianbrains.dev/t/${topic.slug}/${topic.id}`,
    //created_at: topic.created_at
  //}));
};
